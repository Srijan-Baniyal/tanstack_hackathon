services:
  - type: web
    name: meshmind
    runtime: docker
    dockerfilePath: ./Dockerfile
    region: oregon
    plan: free
    dockerBuildArgs:
      - key: VITE_CONVEX_URL
        sync: false
    envVars:
      - key: VITE_CONVEX_URL
        sync: false
    healthCheckPath: /
